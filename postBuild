#!/bin/bash
# Download dataset into "data.zip" archive file 
wget -v -O data.zip https://leicester.figshare.com/ndownloader/files/20031056
# Unzip it to "data" directory
unzip -n data.zip -d ./data
# Remove "data.zip" archive file
rm data.zip